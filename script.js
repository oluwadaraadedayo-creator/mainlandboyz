const track=document.querySelector('.video-track');let down=false,start=0,scroll=0;track.addEventListener('pointerdown',e=>{down=true;start=e.clientX;scroll=track.scrollLeft;track.setPointerCapture(e.pointerId);track.style.cursor='grabbing'});track.addEventListener('pointermove',e=>{if(!down)return;track.scrollLeft=scroll-(e.clientX-start)*1.15});['pointerup','pointercancel'].forEach(x=>track.addEventListener(x,()=>{down=false;track.style.cursor='grab'}));document.querySelectorAll('.video-card').forEach(card=>card.addEventListener('click',()=>{document.querySelectorAll('.video-card').forEach(c=>c.classList.remove('active'));card.classList.add('active')}));